# D03-W01 configured handoff — 5 September 2026

Nguyen reports the origin correction is published and frozen. This supersedes the earlier request to fix the placeholder, not the remaining Codex activation gates.

- Wrapper: `XiXJKuph1fAeH9pe`.
- New frozen revision: `1bce8c54-637e-4770-af90-2da36f38369a` (`issue41-pin-junction-origin`). Do not register the superseded `4fb2f570-705f-40ed-99cd-fdbdf041420b`.
- Origin: `https://junction-unc.vercel.app`.
- Receiver: `POST https://junctionai8.app.n8n.cloud/webhook/unc/d03-w01/keyword-shadow`.
- Reported refusal tests: 73–75; no DataForSEO calls. Receipt shape unchanged; no in-workflow revision lookup.

## Independent observation, approximately 07:48 UTC

Codex opened the existing authenticated Cloud UI read-only. `Wrapper Config` visibly contains the exact canonical origin. Saved [execution 75](https://junctionai8.app.n8n.cloud/workflow/XiXJKuph1fAeH9pe/executions/75) links to the new revision above; runtime 1.773 seconds / 27 KB. Its executed-node log stops at the controlled refusal branch; DataForSEO does not appear. Output is HTTP 401, `missing_data_token`, with `workflowVersion: null` / `revisionEvidence: pending_unc_verification`. The UI's Succeeded means the refusal completed, not keyword success.

Follow-up inspection at approximately 07:54 UTC also confirms executions 73 and 74 bind the same saved revision and stop before DataForSEO. Execution 73 returns HTTP 401 / authority HTTP 401; execution 74 returns HTTP 502 / authority HTTP 503. These are refusal/error paths, not provider success. The saved provider node uses one keyword, a 30,000 ms timeout, Retry On Fail off, Basic Auth credential reference and On Error Continue. Raw credentials were not opened. Settings still has no n8n API item. No new execution, credential change or workflow edit was made by Codex.

The same UI still displays a trial with nine days left. Nguyen's statement that the authenticated execution endpoint exists is not proof of a usable worker credential or plan entitlement. Current [official API authentication documentation](https://github.com/n8n-io/n8n-docs/blob/main/docs/connect/n8n-api/authentication.md) still says the API is unavailable during the free trial and non-Enterprise keys have full account capability. Resolve the actual access/entitlement and owner approval before key provisioning; do not substitute browser cookies for runtime credentials.

Fresh plan-screen check at approximately 07:56 UTC: Starter €24/month billed monthly, Pro €60/month billed monthly, both before applicable taxes. Only the plan screen was opened; no selection, subscription or key creation. This confirms the available commercial choice, not permission to purchase or a successful execution API read.

## Codex-owned next steps

1. Securely reconcile the receiver credential (last worker read found 21 characters, below the bridge's 24-character minimum). Nguyen has not yet confirmed the intended credential's format; no secret should be relayed in chat.
2. Establish supported independent saved-execution access and prove actual snapshot/revision/trigger shape through that reader.
3. Complete durable one-use dispatch/provider admission and uncertain-outcome reconciliation; a reusable read-only authority check is not a paid-call permit.
4. Register only the intended AVGAR run/spec/final revision, deploy matching tested app/worker/schema, and issue the bounded run context.
5. Test `golf travel bag` separately for US/NZ/AU, then independently read the correlated Unc artifact/receipt. Publishing, messaging, ad changes and schedules remain off.

No further Nguyen redesign is requested. This handoff closes the origin correction; full B01–B24 acceptance remains open.
