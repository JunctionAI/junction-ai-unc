# Signed-in customer keyword acceptance — 6 September NZ

PASS for the bounded customer-button → shared command queue → worker → n8n → DataForSEO → saved artifact path. This is not a full-platform launch or a useful-output approval. B03/B24 remain partial for natural-language success, other channels and customer usefulness; the original B01–B24/all-client objective remains active.

## Exact live evidence

| Field | Observed value |
|---|---|
| Account / generation | AVGAR `aa5cfc84-2569-4c99-9b40-67003ae55eda` / 1 |
| Verified owner | `74802c60-149a-4405-b719-dc058d174072` |
| Customer request UUID | `7373b3d9-438f-4267-97d0-1bc2f71dad36` |
| Command and run | `aeb10060-f0c5-508e-a99a-d70e919eea27` |
| Request time → completion | `2026-09-05T16:02:44.052Z` → `16:02:54.917Z` |
| Routine / mode | D03-W01 v2 / dry_run |
| Market / seed | US 2840 / English / `golf travel bag` / `avgarsport.com` |
| n8n execution | [80](https://junctionai8.app.n8n.cloud/workflow/XiXJKuph1fAeH9pe/executions/80) |
| Saved executing revision | `92135add-3c35-43e4-9649-5bb3d4557814` |
| Artifact | `c022a1e4-8e0a-4ae4-924d-b0d7d43c6590` |
| Provider task | `09051602-2436-0607-0000-87f744a15542` |
| Provider response | status 20000; task status 20000; one result; reported cost field `0.01212` |
| Independent request digest | `d75a2913a8de1f90aebd9e2c78b8c838580ebdfbed0a51ec6d5f75959ad0fa45` |

The actual worker's authenticated GET-only execution reader independently reread #80 after completion. It returned the same account/run/routine, revision and request digest as the artifact receipt. A separate saved-execution read inspected `DataForSEO Keyword Overview` (HTTP Request with existing Basic Auth): one successful node run, task ID above, input keywords `["golf travel bag"]`, location 2840 and language `en`. It read the saved provider response's `body`; no extra DataForSEO query was made. The response cost is reported evidence, not yet a durable Unc per-provider cost ledger or a verified currency claim.

Production SQL joins the completed command/run, verified permit, artifact and five correlated receipts. `executedAction=none`, `revisionEvidence=verified_execution_record`; expected and observed revisions match. A draft is not a published website change or SEO outcome.

## Test window and controls

Read-only preflight at 15:59:48 UTC found the reviewed wrapper definition unchanged (`85c5010892e9d6c8d467a69e480ef57307192b23ddf6250f9c0f4561867f58b5`); only the deliberate account pause blocked execution. Owner/US canonical recipe checks passed, with model budget remaining `14.771952` under the existing environment cap of 15. No account commercial budget or ad CPA setting was invented or changed.

The temporary release scope was exactly AVGAR / generation 1 / app / D03-W01, with:

- Spec fingerprint `039d56ca8bd538fb4a4e43ff757ee0fc38de4dcef9a48d9e36fcedc2a5ef5e70`.
- Workflow fingerprint `c446dd03c35b8d93a09f31f7044e0c2820dc34ce1ac5c5381bd963ef67c4abf1`.
- Expiry `2026-09-05T16:20:10.706Z`.

Commands were enabled only with that exact scope. The database setup pause was lifted with owner/generation/saved-state/unresolved-work checks; only the keyword switch was selected through the actual signed-in Agents UI. The canonical recipe is manual, not scheduled. The customer button issued exactly one request. Existing atomic keyword admission supplied its one-use provider authorization; no operator CLI issued this run.

Deployment for the test: `dpl_4tiVwSaE27RcDj5Zs3QR6eaZxLUd`, source `5be9cfe81df31ac8624e55fc56d0a4b7e25b98dc`, Next.js READY, build 37.316 seconds. It was built with production configuration and no domain assignment, health and anonymous-401 checks passed, then promoted. Worker release 27 used the already-tested exact image, not a rebuild. Publishing, customer messaging, SMS, Apple messaging and live/ad changes remained false throughout.

## Customer recovery, duplicate and switch-off evidence

1. Actual request UI first displayed queued, then explicit original-ID inspection displayed done. “Show latest saved result” loaded the new US artifact and its receipts.
2. After switching the routine off through the customer UI (`16:04:35.211836Z`), account chat `/run D03-W01` was refused as switched off while the scoped command flag was still enabled. No second command or run appeared.
3. Replaying the original saved actor/request through the production shared dispatcher returned the existing completed command ID. The interpreter was replaced with a throwing sentinel for this read-only duplicate check and was not invoked. This tests shared production dispatch deduplication, not a second authenticated HTTP POST or a lost transport response.
4. Full app reload after restoring the hold shows four actual runs/results, zero enabled routines, and the setup pause. The original browser journal survived; its GET-only recovery still returns the same done command while paused and global commands are off. No automatic retry occurred.

## Restored state

- AVGAR generation 1 / paused true; keyword v2 US recipe preserved; zero enabled routines.
- One completed customer command, four total runs/artifacts, zero unresolved permits. Original three operator results preserved.
- Canonical app restored to the tested commands-off deployment `dpl_EbKoF9dtfj4TBNVzGdZENAqvygmG`.
- Existing sole Fly machine `1857466fd76998`, release 28, unchanged image digest `1c9280694468f0896e4601e6db58b7206e2d799779ceb17005ef331069f28a45`.
- Actual worker readback: matching source SHA, `UNC_COMMANDS_ENABLED=false`, `UNC_COMMAND_RELEASE_SCOPES=[]`, all external-action flags false. DB and fresh heartbeat healthy.
- Bounded test-deployment error/fatal scans returned no entries. No new log drain or alert delivery verification; previous zero-drain gap remains.

## Remaining acceptance and next work

Nguyen still owns correcting the saved-output quality issues: “DataForSEO-certified” wording, paid competition vs organic difficulty, unsupported homepage target, source-update/currency provenance. Preserve original artifacts as evidence; do not overwrite them to make the test look better. Review and coordinate any new frozen revision before another provider run.

Natural-language success, unattended/durable completion UX, customer-usefulness approval, broader lane adapters, shared data/auth freshness, metrics/cost ledger, schedules, tenant/channel coverage and security/retention remain open. The US button test does not stand in for all three customer-market runs or all routine combinations. No workflow was edited/published, no client credentials were moved, and no contract/payment terms were changed.

## Nguyen coordination

Fresh Upwork read found his 3:02 AM acceptance of the five-lane completion split under the existing agreement and his 3:32 AM report that the output corrections are fixture-tested but unpublished. At 4:09 AM NZ, Codex sent and read back the customer #80 receipt and cleared Nguyen to publish only those output corrections after validation and the three saved-data fixtures. Requested the exact changed-node code/export, corrected outputs, test summary and new frozen revision. This is authorization for Nguyen's output-only revision, not a Codex workflow edit or a provider run. The app/worker remain paused and commands-off until independent review/repinning. No extra charges or contract changes; Email/calendar/inventory stay in his workstream.
