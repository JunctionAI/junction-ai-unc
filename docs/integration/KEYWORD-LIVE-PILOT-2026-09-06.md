# AVGAR keyword pilot — real three-market round trip

**PASS: supervised operator integration. PARTIAL: customer-ready routine.** This closes the first real Unc → n8n → DataForSEO → Unc artifact/receipt round trip, not B01–B24 or the all-client launch. No workflow was edited or published by Codex.

## Release and entry

Source `12b93528b26f9acdf3e3b99a9a2d47babf9c3335`; app `dpl_8Ybx7cqot5wX3WfUF1Z8LArVpBSs`, promoted from `https://junction-5uk7dmo2p-tom-junctionmedis-projects.vercel.app` to `https://junction-unc.vercel.app` after candidate health/anonymous history refusal. Worker release 24 used image `registry.fly.io/unc-worker@sha256:658a5e2eb9acf5927ca1979dc190f7005d335334e919642d4d39120f00b8f356` / tag `deployment-01M1S14596NG4W0MZVMSCZ9F78`; enabling the read-only execution reader completed as release **25**, same image and machine `1857466fd76998`. Actual worker source/pin/reader and all five disabled action flags were read independently. Canonical app/database/fresh-worker health passes; bounded error scan found no entries. Monitoring/retention/security are not globally signed off.

Frozen wrapper `XiXJKuph1fAeH9pe`, revision `92135add-3c35-43e4-9649-5bb3d4557814`, same native Header Auth credential `Y9Xu3zApLSrcWu1e`. Its definition exactly matched the previously reviewed definition with only Ignore Bots false. Migration `20260905145413_keyword_shadow_bot_filter_revision` repinned both service-only SQL functions without changing ownership/grants/security mode. Full 210-file / 2,647-test suite, app/worker typechecks, 36 operator/preflight tests, lint/diff checks and actual production rollback-only issuance/start canaries passed before issuing real permits.

Receiver probe marker `unc-receiver-no-authority-20260905T145800Z` produced saved execution **76**, HTTP 401 `missing_data_token`. Independent readback proved actual webhook mode/new revision and no DataForSEO node. This proves receiver authentication and missing-authority refusal, not a successful business run.

## Actual results

All use `golf travel bag`, English, verified AVGAR domain and generation 1. Each has one immutable allowance, one provider task, one artifact and five receipts, with `executedAction=none` and `revisionEvidence=verified_execution_record`.

| Market | Location | n8n execution | Unc run | Artifact |
|---|---:|---:|---|---|
| US | 2840 | [77](https://junctionai8.app.n8n.cloud/workflow/XiXJKuph1fAeH9pe/executions/77) | `39ed122b-5b29-4e7d-9fae-0d974a8962e9` | `88e4d9be-84ff-48ca-89f3-a6169ae22074` |
| NZ | 2554 | [78](https://junctionai8.app.n8n.cloud/workflow/XiXJKuph1fAeH9pe/executions/78) | `14c8823c-2a45-427d-8286-7ec44af3988a` | `d344c4c1-0b29-4ec0-a2d4-5bc1b95a9554` |
| AU | 2036 | [79](https://junctionai8.app.n8n.cloud/workflow/XiXJKuph1fAeH9pe/executions/79) | `b68396e5-d925-4f9c-8038-84e5f87ca9aa` | `a45fa5a1-f099-4f5d-837e-5266bce73281` |

Registration `a4081614-78a8-445c-912d-0aa8cb9f8081`, account-specific D03-W01 only. Original approval keys/expiry/reference are in [approvals](approvals/); never extend or reissue them. Real provider task IDs: US `09051500-2436-0607-0000-b0f044df5884`, NZ `09051502-2436-0607-0000-dce0a21234a0`, AU `09051502-2436-0607-0000-9c9eec88dae1`. Independent authenticated saved-execution reads confirm webhook mode, exact revision, each actual country/keyword request and matching task/result, with top-level/task status 20000.

Provider-reported `search_volume` was 22,200 / 720 / 3,600 (US/NZ/AU); CPC 1.96 / 0.53 / 0.63; paid competition 1/HIGH throughout; separate keyword difficulty 3 / 0 / 0. Keyword-data update times were 28 / 27 / 19 August respectively, not the September request time. These are provider estimates, not AVGAR traffic/rank or guaranteed outcomes. The raw provider cost field is 0.01212 per task; currency-qualified cost accounting is not yet projected into the artifact/Unc cost ledger.

Each optional Shopify page read returned 39 rows from the existing connection. Search Console was unavailable and explicitly recorded as an optional-read notification; no GSC metrics were fabricated. This does not prove every connector's health or update its freshness badge automatically.

Signed-in production Today after a full reload shows three completed shadow runs, three drafts awaiting review, zero enabled routines and zero runs needing attention. Work inbox/history shows all three country artifacts and corresponding run/receipt identities; opening the US artifact exposes the actual provider data. Historical Approve/Hold/Edit remain disabled. No fake seed records were inserted to populate the dashboard.

## State and remaining work

- Setup pause was lifted only for supervised initial US and then NZ/AU tests, and restored after each completed window. Final AVGAR state: generation 1 / paused; three verified permits, three done runs, three artifacts, 15 receipts; no routine switches or schedules enabled. Worker execution reader is now true; command/messaging/live/TNZ/Apple action flags remain false. Vercel's general execution-reader flag remains false; this pilot dispatch was worker/operator-only, while the canonical app served the run authority and saved UI.
- **Nguyen output correction:** replace “DataForSEO-certified”; retain provider update time and verified metric currency; do not treat paid competition as SEO difficulty; explain prioritization or leave it pending; do not label the homepage a verified target page without page evidence. Actual saved builder code uses paid competition thresholds for priority and constructs target_page from the domain. Existing source data is enough to test corrections without refetching. Keep current published revision frozen until a coordinated new pin is agreed.
- **Codex:** real customer Ask/switch/manual dispatch still needs its own scoped issuance/selection acceptance. This operator entry does not prove B03 or combinations work. Later lane adapters, stored-data synchronization, multi-client/channel/security/retention and governed review remain open. Improve plain-language rendering (raw underscores currently become markdown emphasis) and country labels. Do not silently overwrite these original evidence-bearing drafts.
- **Tom:** usefulness review and remaining non-inferable business inputs/rights. No additional provider access purchase or API key is needed for this proven keyword path.

The three existing-pilot SQL canaries were designed for an empty paused pilot. **Do not rerun those fixture scripts against the now-populated production account.** Use isolated fixtures for subsequent changes. Do not roll the worker pin back while keeping the new database pin, and never rewrite historical execution revisions to match a later workflow.

Provider metric semantics: [DataForSEO's own keyword-overview explanation](https://dataforseo.com/help-center/getting-keyword-data-with-keyword-overview-and-historical-keyword-data-endpoints) distinguishes paid-search competition. The migration's reconstruction mechanism follows [PostgreSQL function introspection](https://www.postgresql.org/docs/17/functions-info.html); its own assertions prove only the intended literal replacement.
