# Bounded AVGAR keyword pilot authority

Tom explicitly requested completion and testing of the Unc/n8n read/draft pilot, authorized the required account/backend setup, and supplied `golf travel bag` separately for US/NZ/AU. This record scopes that existing instruction; it is not a new approval attributed to him, a new spending agreement or authority to run other routines.

Codex will issue at most one initial DataForSEO allowance per named market against workflow `XiXJKuph1fAeH9pe`, independently verified frozen revision `92135add-3c35-43e4-9649-5bb3d4557814`, account `aa5cfc84-2569-4c99-9b40-67003ae55eda`, owner `74802c60-149a-4405-b719-dc058d174072`, generation 1. Each allowance has its own original key and expiry within ten minutes. Start with US; inspect its result before authorizing the separate NZ/AU calls. No uncertain request is retried under a new key.

Preconditions verified: matching app/worker release; authenticated execution-read access; actual receiver-authenticated missing-token refusal #76 with no provider node; new SQL pin and rollback-only issuance/start canaries; zero enabled routines, commands and manual requests. The server execution reader may be enabled for independent verification. No channel, command, publishing, messaging, ad mutation or spend-activation flag is enabled.

The Codex-imposed setup pause may be lifted for this supervised acceptance window after those checks, then restored while reviewing the original result. Do not bypass pause checks, weaken tenant/generation guards, enable a routine or add schedules. This is not general customer readiness. Account identity/credentials/commercial fields stay unchanged.

`approvals/avgar-keyword-US-20260905-1500-v1.json` records the first US attempt. The intentionally bounded expiry must not be extended once issued. If its result is uncertain, inspect that exact key and saved execution before any further provider activity.
