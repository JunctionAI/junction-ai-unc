# Agents and Connections — Batch 31

Previous goal turn: Batch 30 verified ops release, not blocked. Full B01–B24 and all-screen/all-client acceptance remain active. No n8n purchase/key authorization, workflow modification or client activation is inferred.

## Implemented journey

- Supplied Agents area groups, individual switches, filters/search and inspection use 35 existing stable routine IDs. `src/lib/agents/catalog.ts` preserves all 38 original design labels explicitly. Research/scoring share one switch; creative testing sprints remains a distinct existing routine that the design omitted. Three unsupported design jobs (competitor-ad monitoring, automated lead calls, industry positioning) plus Google Ads BOFU and referring-domain/backlink gap are visible but unavailable, not assigned invented IDs. This produces 40 displayed jobs, 35 mapped IDs, five unavailable additions. No combination-specific workflows.
- Outcome wording is bounded by current behavior: recommendations do not move spend; drafts do not send/publish; welcome-flow tuning does not prove repeat purchases; AI-search planning is not a live probe or confirmed citation; competitor-page analysis is not backlink gap. Nguyen's proven routines still need their own accepted contracts and account-specific integration; labels do not supply that evidence.
- New GET/POST `/api/agents` requires the signed-in account plus exact account/generation correlation headers. GET refreshes membership, pause, saved revision and current-generation recent run/receipt history. It rejects demo fallback and does not return provider tokens. The browser discards failed/foreign listings and blocks mutation while loading, saving or read-only.
- A switch only saves a preference; the endpoint does not run a workflow or create a schedule. Run inspection remains available through the existing inspector. Existing schedulers and actual execution eligibility still require separate acceptance; selecting a routine does not certify it running. The reserved D03-W01 keyword pilot cannot be enabled through this general endpoint: its operator-only issuance/registration gate remains separate.
- Service-only `set_agent_switch` locks/checks account generation, current owner membership, pause and expected routine timestamp/version, then changes only the selected preference. Off remains available while paused; on does not. A missing row is initialized only against a missing-row expectation. Concurrent/stale updates fail instead of overwriting. The browser does not automatically retry an uncertain POST; it reads current state before another user change. The account autosave already excludes routine/connector rows, so the UI mirror cannot write old switches back through that path.
- Connections ports the supplied two-tab layout onto the existing data and channel controls. Data cards use real connector state rather than prototype flags; selected asset, dated read and auth recovery are explicit. Read counts require selected assets plus dated successful/empty reads, and are not a freshness guarantee. Prototype “unlocks N routines” counts are omitted. Healthy selected assets no longer automatically request a picker list on every modern-view visit. Failed listing clears management controls instead of showing demo success. Existing provider-specific OAuth/token/selection/disconnect paths are reused, not reinstalled or retested by this visual port.
- Messaging reuses the existing account-bound setup panel, with an explicit disabled-delivery/unfinished-deep-Slack notice. No fake routing owners, successful phone deliveries, digests or escalation switches are copied from the design.

## Evidence before app release

- Full unit suite: **205 files / 2,575 tests PASS**. App and worker type checks, production build and diff check PASS. Lint: zero errors / 41 existing warnings; no suppressions or dependencies added.
- Eight new browser cases cover single/combined/all-off persistence and reload with no run POST; catalog aliases/unavailable jobs; owner/member/pause; conflict and lost-response reconciliation without retry; foreign response denial; Connections evidence/error-state and Messaging notice; 390/1280px Agents and 390px Connections. Fourteen existing workspace/ops browser cases also pass. Fixtures are isolated synthetic state, not real provider acceptance. Initial test setup incorrectly seeded duplicate fake rows; corrected it to update/delete rows, preserving duplicate-key enforcement.
- Real PostgreSQL rollback canary passes before and after applying the function: public/member RPC privilege denial; wrong tenant/actor/member; stale generation/timestamp/version; paused enable refusal and paused off success; exactly one changed preference, no unrelated rows or runs. All canary accounts/memberships/states roll back; independent remaining count zero. This is sequential conflict evidence, not a live multi-session race test.
- Source migration `20260905112730_routine_switch_context.sql`; live version **20260905113720** / `routine_switch_context`. No table grant or RLS policy broadened; function is SECURITY INVOKER, public/anon/authenticated execution revoked, service_role only. Post-apply security advisors remain six WARN / fourteen INFO, no new function finding. Existing advisories/security sign-off remain open.
- Independent SQL at **11:37:52 UTC**: seven accounts, two memberships, 19 connector rows, zero runs and enabled routines; active AVGAR paused. Anonymous/authenticated function execute false; service-role true. No production preference, business context, credential, channel binding, provider data or customer message changed.

## Remaining gates / compatibility

- App deployment, actual signed-in navigation and canonical health readback pending at this checkpoint. Rollback target is Batch 30 `dpl_2AWqiFLsPJTpDT3h4m5bwJ2fVgYF`, source `170c765a9c8180cfc92650fd40ecf01bef056258`. Additive unused function need not be removed for UI rollback.
- No worker contract or dependency behavior changes: existing callers omit the new optional history-generation argument and retain their behavior. Keep compatible Batch 27 worker rather than restarting just to match frontend SHA. Its unchanged source is not evidence of broader worker acceptance.
- General/legacy switch and inspector/setup paths still need consolidation with the new CAS endpoint; this batch does not close all routine-selection races. Effective promoted-spec readiness, canonical integration receipts, actual schedule presentation and pagination (recent history is bounded at 300) remain open. A stored run status is not independent n8n verification.
- Connections reuses older auth handlers and their remaining hosted/manual/picker/shared-grant/disconnect races; no claim of complete OAuth or renewal acceptance. Data freshness/provider scope and the saved-state identity boundary still require complete end-to-end acceptance on each client. Existing-agent canonical binding, five missing memberships, phone/deep-Slack delivery, other lane contracts, costs/retention/security and the original backend gates remain open.
- Pending n8n supported API entitlement/key authority and receiver reconciliation are unchanged. No repeated blocked n8n probe, workflow edit, provider/model call, secret rotation or purchase this batch.

Design/React guidance preserved the supplied information hierarchy and added actual loading/error/disabled states. Supabase guidance led to a service-only atomic switch boundary rather than browser-authorized cross-account writes.
