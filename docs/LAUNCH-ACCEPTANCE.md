# Real platform and all-client acceptance

Tom's 5 September clarification is the delivery bar: every agreed screen must use real backend functionality; each client must be connected to the correct existing working systems with visible freshness; Codex identifies and owns the remaining technical gaps. The original B01–B24 goal stays active. This document extends its acceptance coverage; it does not delete unfinished work or claim that screenshots/tests equal completion.

## Delivery and token discipline

1. Work by complete customer journey, not endless isolated hardening. Reuse existing contracts, code, sessions and proven infrastructure. No new architecture or provider swap without a concrete failing requirement.
2. Maintain one status per journey/client: PASS with a live receipt, PARTIAL with the exact missing step, or BLOCKED with the dependency/owner. Do not compute completion from test count or a list of seeded accounts.
3. Run focused tests during editing; one full test/type/build batch before a coherent release. Reuse unchanged evidence and combine related work into releases. Do not repeatedly inspect unchanged blocked systems.
4. Continue authorized independent work while Nguyen or owner approval is pending. If only external authority remains, report it precisely and stop burning tokens on the same checks. No new numeric token budget was supplied.
5. Keep publishing, customer messages, ad mutation and spend activation disabled until separately authorized. An honest disabled state is not evidence that the requested capability is implemented; it remains an open gate.

## Screen-to-outcome acceptance

| Surface / journey | Current evidence | Codex-owned next deliverable | Completion proof |
|---|---|---|---|
| Landing | PASS for v2 port and core signup journey: Batch 28 production form → saved record, duplicate retry and scoped cleanup independently verified | Broader abuse/retention checks remain; move implementation to client screens | [Live receipt](LANDING-SIGNUP-2026-09-05.md); fixture tests cover failed/invalid confirmations without false success |
| Client Today | Existing old UI uses account data; new design not ported | Bind new layout to dated results, actual setup and upcoming work | Reloaded page matches account/runs/receipts; no fabricated metrics or running states |
| Client Work inbox | Artifact and approval APIs exist; new layout not ported | Wire drafts, edits, hold and approval according to their distinct authorities | Correct tenant/revision persists across reload; no unintended external action |
| Client Agents | 38 design labels versus 35 executable IDs, not a one-to-one delta | Reconcile every offered job, then wire persisted switches and readiness | All-off/single/combined selections execute only selected eligible work and preserve dependencies |
| Client Ask | Existing account chat; complete routine-to-result journey unproven | Bind new Ask screen to persisted conversation and certified dispatch/results | One plain-language request produces one correct account-scoped result and reloadable receipt |
| Client Connections/channels | New auth safety batch live; AVGAR historical Meta/Shopify reads; phone/Slack acceptance incomplete | Bind new surface to existing grants, asset selection, refresh/read/freshness and verified channel identity | Fresh authorized reads, reconnect/recovery and channel-routing acceptance per required integration |
| Ops Clients/client detail | New prototype only; no cross-client operator API | Add explicit operator authorization and selected-client projections; reconcile canonical client identities | Authorized operator sees the right client; ordinary member cannot access another tenant |
| Ops Setup pipeline | Prototype only | Track actual connection/config/access tasks without demo completion | Each status derives from evidence; real next action and owner are visible |
| Ops Run monitor | Existing tenant run/receipt primitives; no new ops implementation | Wire actual run status, failures, costs and safe recovery | Monitor matches saved executions/artifacts; uncertain work is reconciled, not blindly repeated |
| Existing clients' systems | Account inventory below; legacy working systems not yet reconciled with Unc | Inventory existing deployments/routes/credentials without copying grants or re-installing; bind each to canonical tenant | Each in-scope client passes sign-in, fresh context/read, chosen job, result, reload/history and switch-off |

## Read-only Unc account inventory — 5 September, approximately 10:26 UTC

These are database records, not verified active-client readiness. Repository beta seed files contain these client names. Existing agents outside this Unc database may work; this inventory does not assess or replace them.

| Account | ID | Login members | Connector rows | Dated successful selected bindings | Saved runs |
|---|---|---:|---:|---:|---:|
| Unity MMA | `083d6e9f-4697-4845-8869-952c0e1f9d83` | 0 | 0 | 0 | 0 |
| Deep Blue Health | `4fae2ad8-194f-49c4-9954-39a6b6d35b14` | 0 | 5 | 0 | 0 |
| Home Invasion | `1620999d-ee8e-4490-bfd6-b908a9fa86dc` | 0 | 3 | 0 | 0 |
| Rory O'Keefe | `b6c1749a-8604-4d9c-92a3-6995f6c40fa7` | 0 | 0 | 0 | 0 |
| Aerspan Airdomes | `f206666f-ac52-4508-a508-3535a8f229a3` | 0 | 0 | 0 | 0 |
| AVGAR Sport — other record | `a37ddf37-489c-491f-8d30-23a4f1f38fc4` | 1 | 5 | 0 | 0 |
| AVGAR Sport — active pilot | `aa5cfc84-2569-4c99-9b40-67003ae55eda` | 1 | 6 | 2 | 0 |

The active pilot is generation 1 / paused. Its two dated reads are Meta and Shopify from 3 September, not new provider reads. **19 connector rows and two sealed credentials are database-wide counts, not 19 active AVGAR connections.** Do not merge/delete the other AVGAR record, assign memberships or invite clients based on display-name equality. First reconcile verified owner/asset/system identity and preserve history.

## Immediate work order and owners

- **Codex:** Batch 28 landing/signup is released and its core journey verified. Port the supplied client screens against existing APIs next. Reconcile all job labels and build explicit ops authorization. Inventory the already-working client systems and canonical account bindings rather than asking Tom to repeat technical setup. Continue the original auth/data/schedule/security/acceptance gaps where required by these journeys.
- **Nguyen:** unchanged frozen D03-W01 wrapper; provide five-lane routine/metric/result mappings and per-client credential ownership evidence as lanes are packaged. No combination-specific workflow copies or additional wrapper redesign requested.
- **Pending owner/platform dependency:** n8n trial lacks supported API access. The existing question about upgrade cost and a server-stored account-wide read/write key remains unanswered; app GET-only use is not provider-enforced read-only. Receiver-secret compatibility/equality and independent saved-execution verification remain prerequisites to authorized US/NZ/AU `golf travel bag` runs. Do not purchase, mint or rotate a credential without the missing approval.
- **Tom:** only decisions that cannot be inferred: provider fees/key authority, genuine owner consent/rights, commercial settings and useful-output acceptance. No need to reset the active goal simply to continue. No public-release or customer-message approval is inferred from wanting the platform complete.

Not complete until the agreed screen and client journeys pass on deployed code. Nguyen's delivery alone cannot close the frontend, ops authorization, account reconciliation or client acceptance work owned by Codex.
