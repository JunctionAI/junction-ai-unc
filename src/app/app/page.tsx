export default function Platform() {
  /* Placeholder — the Platform v2 port (onboarding + control centre) lands here next. */
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "var(--muted)",
        fontSize: 14,
      }}
    >
      Unc is on his way — the control centre port is in progress.
    </div>
  );
}
